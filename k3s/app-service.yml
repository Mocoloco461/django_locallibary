apiVersion: v1
kind: Service
metadata:
  name: locallibary
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/locallibary: locallibary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000ד