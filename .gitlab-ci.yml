stage:
  - build
  - push


run_docker:
  stage: build
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  script:
    DATABASE_URL=postgres://${POSTGRES_USER_TEST}:${POSTGRES_PASSWORD_TEST}@db:5432/${POSTGRES_DB_TEST}
    docker compose -f docker-compose-test.yaml up --build --exit-code-from app_libary
